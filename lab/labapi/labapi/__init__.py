"""lab-api: the scenario runner (docs/LAB.md). Python, FastAPI, one shape for
every endpoint: the runner owns the state, lab-driver polls, Proxmox and the
server are reached through small interfaces that tests replace with fakes."""

__version__ = "0.0.1"
