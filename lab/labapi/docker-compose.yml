# Superseded by lab/deploy/sky-srv/docker-compose.yml, the file that actually
# lands on sky-srv (paths under /srv/lab, images from the sky-ci registry).
# Kept as a pointer so old references still find the truth.
