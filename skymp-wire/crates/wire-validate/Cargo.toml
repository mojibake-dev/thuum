[package]
name = "wire-validate"
version = "0.0.1"
edition.workspace = true
license.workspace = true
description = "Structural validation of decoded messages: bounds, rates, owner shape."

[dependencies]
wire-schema = { path = "../wire-schema" }
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
